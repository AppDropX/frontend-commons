/// Fixed viewport height for `display_mode: embedded` custom HTML blocks.
///
/// Used by builder theme preview, [pilot], and [appdrop_preview] so embedded
/// blocks always occupy the same slot size on every page.
const double kEmbeddedCustomBlockHeight = 300;
